# AdditionalDocSpecs — <ProjectName>-Specific Documentation Constraints

*Created: YYYY-MM-DD*

Optional documentation-specific constraints that extend `./DocSpecs.md` for
`<ProjectName>`. Delete every `<placeholder>` once filled in; delete a whole
section if this project genuinely has nothing to add for it.

---

## Chapter / document list

<A table: document file → one-line description → where it sits in the
build order (e.g. what a top-level `MASTER.tex`/`main.tex` `\input`s, and
in what sequence).>

## Glossary

<A table of this project's domain-specific terms a documentation writer
needs, each defined once and pointed at from here — not redefined
differently in multiple chapters.>

## Dev-facing documentation (if separate from the book)

<If this project also has a dev-facing doc set alongside the user-facing
book (e.g. an architecture/internals folder), name it here and state how
it differs in audience and scope from the chapters listed above.>
