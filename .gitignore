# DevSpecs.md's Planning section (Locality) requires a consuming project's
# planning tickets to stay local to that project's own repository, never
# synced back to a shared spec repo. This is a second line of defense in
# case a contributor ever runs project tooling from inside a ./DocSpec/
# submodule checkout by mistake.
AgentSpecs/
*_DevPlanTicket.md
